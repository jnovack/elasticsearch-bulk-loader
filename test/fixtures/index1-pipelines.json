{
  "e2e-index1-calc": {
    "description": "Calculate derived fields for source documents before indexing",
    "processors": [
      {
        "script": {
          "lang": "painless",
          "source": "ctx.calculated_value = ctx.base_value * ctx.multiplier; ctx.source_label = ctx.source_name + '-' + ctx.calculated_value;"
        }
      }
    ]
  },
  "e2e-index1-secondary": {
    "description": "Secondary source pipeline that is not the default pipeline",
    "processors": [
      {
        "set": {
          "field": "secondary_pipeline_ran",
          "value": true
        }
      }
    ]
  }
}
